apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: runner-ubuntu
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/sosotechnologies/runners.git'
    targetRevision: HEAD
    path: argo-artifacts
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
